platform: linux/arm64

pipeline:
  docker:
    dockerfile: Dockerfile
    image: plugins/docker:linux-arm64
    repo: yukimochi/container-aria2
    secrets: [ docker_username, docker_password ]
    tags: latest-aarch64
